diff --git a/packages/shared/lib/constants.ts b/packages/shared/lib/constants.ts
index 6cb3df3353..29913c52b0 100644
--- a/packages/shared/lib/constants.ts
+++ b/packages/shared/lib/constants.ts
@@ -91,6 +91,14 @@ export const APPS = {
     PROTONCONSOLE: 'proton-console',
 } as const;

+export const ELECTRON_MAIL_APPS = [
+    APPS.PROTONACCOUNT,
+    APPS.PROTONCALENDAR,
+    APPS.PROTONDRIVE,
+    APPS.PROTONMAIL,
+    APPS.PROTONVPN_SETTINGS,
+] as const;
+
 interface AppConfiguration {
     publicPath: string;
     subdomain: string;
@@ -107,7 +115,7 @@ interface AppConfiguration {

 export const APPS_CONFIGURATION: { [key in APP_NAMES]: AppConfiguration } = {
     [APPS.PROTONACCOUNT]: {
-        publicPath: '',
+        publicPath: '/account',
         subdomain: 'account',
         name: 'Proton Account',
         bareName: 'Account',
@@ -219,8 +227,8 @@ export const APPS_CONFIGURATION: { [key in APP_NAMES]: AppConfiguration } = {
         product: Product.Pass,
     },
     [APPS.PROTONVPN_SETTINGS]: {
-        publicPath: '',
-        subdomain: '',
+        publicPath: 'account/vpn',
+        subdomain: 'account',
         name: VPN_APP_NAME,
         bareName: VPN_SHORT_APP_NAME,
         clientID: 'web-vpn-settings',
--
